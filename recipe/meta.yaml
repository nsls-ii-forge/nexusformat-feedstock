{% set name = "nexusformat" %}
{% set version = "0.7.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 6606208210459b1638c7f2bd72419fa0aab55df668c5e6d0d1e66168899fbd8e

build:
  number: 0
  entry_points:
    - nxstack=nexusformat.scripts.nxstack:main
    - nxduplicate=nexusformat.scripts.nxduplicate:main
    - nexusformat=nexusformat.scripts.nexusformat:main
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - h5py
    - ipython
    - numpy
    - scipy

test:
  imports:
    - nexusformat
    - nexusformat.nexus
    - nexusformat.scripts
  commands:
    - nxstack --help
    - nxduplicate --help
    - nexusformat --help

about:
  home: https://nexpy.github.io/nexpy
  license: BSD-3-Clause
  license_family: BSD
  license_file: COPYING
  summary: 'nexusformat: Python API to access NeXus data'
  doc_url: https://nexpy.github.io/nexpy
  dev_url: https://github.com/nexpy/nexusformat
